apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: minecraftdata
  name: minecraftdata
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
